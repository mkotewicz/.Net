@{
    ViewBag.Title = "Home Page";
}

<div class="row">
        @if (User.Identity.IsAuthenticated)
        {
        <p>Dane pracowników</p>
        <br>
        <a href="@Url.Action("Index","SekretarzAdres")">@Helper.globalny("Adresy")</a>
        <br><br>
        <a href="@Url.Action("Index","SekretarzPracownik")">@Helper.globalny("Pracownicy")</a>
        <br><br><br>
        <a href="@Url.Action("SignOut","Home")">Wyloguj</a>
        }    
</div>